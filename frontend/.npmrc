# Vite 8 is newer than the peer ranges some plugins (@tailwindcss/vite,
# @vitejs/plugin-react) currently declare. They work fine at runtime, so we
# relax peer resolution rather than pin older, incompatible toolchain versions.
legacy-peer-deps=true
